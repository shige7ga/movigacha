# CLAUDE.md

アプリの概要・機能・デプロイ先は README.md を参照すること。

## 使用技術

- Ruby 3.3
- Rails 7.2
- PostgreSQL
- Docker（ローカル開発環境）
- Render（デプロイ先）

## やらないこと

- ログイン機能（認証）は実装しない
- 複数ユーザーへの対応はしない

## 作業ルール

- 変更を加える場合は、git のブランチを切ってから作業すること
- 機能が完成したら「ここまでを保存しますか？」と確認し、git add・commit を実行すること
- git push はユーザーから「git push してください」と依頼されてから実行すること
- git push を実行した後は、リモートリポジトリへのプルリクエスト内容を日本語で提示すること
- 「git pull してください」と指示されたら、main ブランチに切り替えて pull し、不要になったブランチ（ローカル・リモート両方）を削除すること
- 作業の方針や指示に変更があった場合は、都度このファイル（CLAUDE.md）に追記すること

## Webアプリ運用ルール

- Webアプリの状態が更新された場合、README.md の情報と相違がないか確認し、相違があれば最新の状態に更新すること

## 環境固有の注意事項

Windows（WSL2）環境で開発しているため、以下のコマンドを実行した後はターミナルで
`sudo chown -R $USER:$USER .` の実行をユーザーに促すこと：

- `rails generate`
- `db:migrate`
